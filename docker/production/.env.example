# NeuroQuantumDB Production Environment Configuration
# Copy this file to .env and customize the values
#
# SECURITY: Never commit .env to version control!

# ============================================================================
# REQUIRED SECURITY SETTINGS
# ============================================================================

# JWT Secret for token signing (REQUIRED - minimum 32 characters)
# Generate with: openssl rand -base64 48
NEUROQUANTUM_JWT_SECRET=

# Admin API key for administrative operations (recommended)
# Generate with: openssl rand -hex 32
NEUROQUANTUM_ADMIN_KEY=

# ============================================================================
# OPTIONAL CONFIGURATION OVERRIDES
# ============================================================================

# Server configuration
# NEUROQUANTUM_HOST=0.0.0.0
# NEUROQUANTUM_PORT=8080

# Logging
# RUST_LOG=info
# NEUROQUANTUM_LOG_LEVEL=info
# NEUROQUANTUM_LOG_FORMAT=json

# Rate limiting (requests per hour per user/IP)
# NEUROQUANTUM_RATE_LIMIT=10000

# Maximum request payload size in bytes (default: 5MB)
# NEUROQUANTUM_MAX_PAYLOAD_SIZE=5242880

# ============================================================================
# CUSTOM CONFIGURATION FILE
# ============================================================================

# Path to custom configuration file (relative to docker-compose.yml)
# NEUROQUANTUM_CONFIG_FILE=./config/prod.toml

# ============================================================================
# MONITORING CONFIGURATION
# ============================================================================

# Grafana admin password
GRAFANA_ADMIN_PASSWORD=neuroquantum2024

# Grafana public URL (for links in notifications, etc.)
# GRAFANA_ROOT_URL=http://localhost:3000

# ============================================================================
# TLS/SSL CONFIGURATION (for load balancer)
# ============================================================================

# Path to TLS certificate and key (required if using load balancer with HTTPS)
# TLS_CERT_PATH=/path/to/certificate.crt
# TLS_KEY_PATH=/path/to/private.key
